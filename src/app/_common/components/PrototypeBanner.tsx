export default function PrototypeBanner() {
    return (
        <div className="prototype-banner" role="status">
            ⚠️ Dette er en prototype — ikke den virkelige arbeidsplassen.no
        </div>
    );
}
